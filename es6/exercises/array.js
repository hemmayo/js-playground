// Reduce
const array1 = [2, 5, 6, 7, 8]
const b = array1.reduce((accumuator, num) => accumuator + num, 0)
console.log(b)

// Map
var array2 = [1, 4, 9, 16];
// pass a function to map
const map1 = array2.map((x) => x + 2);

console.log(map1);